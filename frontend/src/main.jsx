import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';

// TODO: bọc App bằng BrowserRouter, QueryClientProvider, AuthProvider

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <div>Setup thành công. Bắt đầu code App tại src/App.jsx</div>
  </React.StrictMode>
);
