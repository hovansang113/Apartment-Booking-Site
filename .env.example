# Dung boi docker-compose.yml (chi mysql root password) - copy thanh `.env`
# o cung thu muc nay (cung cap voi docker-compose.yml) truoc khi chay
# `docker compose up -d`. Rieng backend/frontend van co .env.example rieng.
MYSQL_ROOT_PASSWORD=doi_mat_khau_nay_khi_len_production

# Domain/IP public that cua server (vd http://154.91.1.216 hoac
# https://domain-that-cua-ban, KHONG co dau / cuoi) - phai khop dung
# CLIENT_URL trong backend/.env, dung de bake vao index.html luc build frontend.
SITE_URL=http://localhost
